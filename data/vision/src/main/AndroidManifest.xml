<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed once, on first run, to download the MediaPipe Pose Landmarker
         .task model file (see PoseModelProvider) - there is no bundled/
         offline-only alternative for MediaPipe Tasks models. -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
