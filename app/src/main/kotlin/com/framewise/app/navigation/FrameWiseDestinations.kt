package com.framewise.app.navigation

/**
 * Route constants for the app's single NavHost. Kept as a plain object for
 * now (Phase 2 only has one real screen); will grow into typed routes with
 * navigation-compose's type-safe API once settings/tips/before-after screens
 * land in later phases.
 */
object FrameWiseDestinations {
    const val CAMERA_ROUTE = "camera"
}
